<p style="color: green"><%= notice %></p>

<%= render @product %>

<div>
  <%= button_back "Back", admin_products_path %>
  <%= button_edit "Edit", edit_admin_product_path(@product) %>
  <%= button_delete "Destroy", [:admin, @product] %>
</div>
